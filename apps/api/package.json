{
  "name": "@bilty/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/domain.test.ts",
    "test:integration": "node --import tsx --test test/postgres.test.ts",
    "db:migrate": "node --import tsx src/database/migrate.ts"
  },
  "dependencies": {
    "@bilty/shared-types": "workspace:*",
    "@nestjs/common": "^11.1.6",
    "typeorm": "~0.3.28",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
