# Complete V1 Implementation Plan

> Use superpowers:subagent-driven-development for independent frontend work and a review gate; continue backend tasks locally.

**Goal:** Complete the usable local V1 against the approved PRD.
**Architecture:** Existing Nest API plus Next App Router frontend; server PDFKit rendering and hashed/revocable PDF shares.
**Spec:** ../specs/2026-09-21-complete-v1-design.md

- [x] Frontend task: apps/web only. Build login/callback/invite/setup, responsive app shell, full bilty entry/detail/list, parties, company and employee administration. Consume documented APIs. Implement strict decimal conversion, full PUT and conflict handling, serialized memory-only sessions. Add meaningful tests for conversion and auth handling. Verify typecheck and Next production build.
- [x] Documents task: apps/api print/share modules and migration. Add failing regression tests for valid PDFs in both formats and long reference collections, tenant-scoped share creation, fixed snapshot version, expiry/revocation/cancelled rejection; implement renderer and secure share endpoints.
- [x] Integration task: update workspace/Docker build and compose, wire frontend PDF/share controls, implement isolated Playwright E2E tests with external Google provider substituted only in test harness. Verify actual browser flows and render PDFs.
- [x] Completion task: reconcile any concurrent repo edits, sync docs, run pnpm check/test:docker/E2E, Docker smoke, independent review and fix concrete findings. Leave local working tree reviewable and provide running application URL plus required live credentials.

Ruling: Complete local Docker V1 first; public hosting requires a separately supplied target and credentials. Frontend uses the existing Claude export as a visual reference, not production source. No production login bypass will be added.

## Verified completion — 22 September 2026

Implemented in the repository, with the prior frontend scaffold preserved separately at the user’s request. `pnpm check` passes (26 API/domain/PDF tests and 6 frontend tests, typechecks and production builds); `pnpm test:docker` passes 36 integration tests; `pnpm test:e2e` passes all 5 browser journeys. Docker PostgreSQL/API/web are healthy at ports 55432/3000/3001; migrations are applied. See docs/verification.md for the verification record and live Google setup boundary.
